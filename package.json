{
  "name": "stela-site-vitrine",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine Stela (www.mystela.fr) — Astro, statique, SEO/GEO first.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/indexnow-ping.mjs",
    "preview": "astro preview",
    "check": "npm run lint:copy && npm run check:brand && npm audit --audit-level=high && astro build && npm run check:animations && npm run check:links && npm run check:schema && npm run check:stripe && npm run check:alt && npm run check:channels && npm run check:analytics && npm run check:partner && npm run check:cities && npm run check:devis && npm run check:cookies && npm run check:overflow && npm run check:contraste && npm run check:diagnostic",
    "lint:copy": "node scripts/check-copy.mjs",
    "check:brand": "node scripts/check-brand.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:schema": "node scripts/check-schema.mjs",
    "check:stripe": "node scripts/check-stripe.mjs",
    "check:alt": "node scripts/check-alt.mjs",
    "check:animations": "node scripts/check-animations.mjs",
    "check:channels": "node scripts/check-channels.mjs",
    "check:partner": "node scripts/check-partner.mjs",
    "check:cities": "node scripts/check-cities.mjs",
    "check:analytics": "node scripts/check-analytics.mjs",
    "build:guide": "node scripts/build-guide-pdf.mjs",
    "indexnow": "node scripts/indexnow-ping.mjs",
    "check:overflow": "node scripts/check-overflow.mjs",
    "check:contraste": "node scripts/check-contraste.mjs",
    "check:cookies": "node scripts/check-cookies.mjs",
    "check:devis": "node scripts/check-devis.mjs",
    "check:diagnostic": "node scripts/check-diagnostic.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-satteri": "^0.3.4",
    "@astrojs/sitemap": "^3.7.0",
    "@fontsource/plus-jakarta-sans": "^5.2.10",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "simple-icons": "^16.27.1"
  }
}
